(ns clj-chorebot.models.chore_history
  (:require [clj-chorebot.config :as config]
            [clojure.java.jdbc :as jdbc]
            [java-jdbc.sql :as sql]))

; TODO (create chore_id user_id)
; TODO (complete_chore chore_id user_id)
